apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gateway-ingress
  annotations:
    nginx.ingress.kubernetes.io/auth-response-headers: "Authorization"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/http01-ingress-class: "traefik"
spec:
  ingressClassName: traefik
  rules:
    - host: api-obshaga-service.ru
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 80
          - path: /users
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 80
          - path: /sessions
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 80
          - path: /roles
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 80
          - path: /contacts
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 80
          - path: /bookings
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 80
          - path: /balance
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 80
          - path: /documents
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 80
          - path: /schedules
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 80
          - path: /responsibles
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 80
          - path: /notifications
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 80
          - path: /grafana  # Путь для Grafana
            pathType: Prefix
            backend:
              service:
                name: grafana-external
                port:
                  number: 80
  tls:
    - hosts:
        - api-obshaga-service.ru
      secretName: gateway-tls-secret